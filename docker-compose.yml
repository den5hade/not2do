version: '3.8'
services:
  mongo: 
    image: mongo:7.0
    environment:
      MONGO_INITDB_ROOT_USERNAME: dshaderkin
      MONGO_INITDB_ROOT_PASSWORD: 7D5f5gH@&mDb
    ports:
      - 27016:27017
    volumes:
      - mongodata:/data/db
volumes:
  mongodata:
    driver: local